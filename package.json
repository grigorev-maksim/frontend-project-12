{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Chat is simplified clone of Slack communication service. It allows you to create new channels, delete and rename them, add new users and chat with them.",
  "main": "frontend/src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx start-server -s ./frontend/build",
    "build": "npm run build --prefix frontend",
    "postinstall": "cd frontend && npm ci"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^1.1.8",
    "react-i18next": "^13.3.1",
    "yup": "^1.3.2"
  }
}
